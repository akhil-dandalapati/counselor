import React from "react";

const HeadindSubheading = ({ subtitle, title }) => {
  return (
    <div className="services">
      <h4>{subtitle}</h4>
      <h1>{title}</h1>
    </div>
  );
};

export default HeadindSubheading;
